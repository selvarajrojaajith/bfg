
#include <iostream>
using namespace std;
 
int getClosest(int, int, int);
int Close(int arr[], int n)
{
    if (target <= arr[0])
        return arr[0];
    if (target >= arr[n - 1])
        return arr[n - 1];
{
    int i = 0, j = n, mid = 0;
    while (i < j) {
        mid = (i + j) / 2;
 }
 }
        if (arr[mid] == target)
            return arr[mid];

}
 
